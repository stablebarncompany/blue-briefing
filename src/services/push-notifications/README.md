# Push notification services

Client helpers for Expo push device registration, preferences, and deep-link response handling.

- Native only (iOS/Android). Web uses in-app notifications exclusively.
- Never store or log service-role keys or full push tokens.
- Sending is performed by `supabase/functions/send-push-notification`.

See `docs/PushNotificationsMVP.md`.
